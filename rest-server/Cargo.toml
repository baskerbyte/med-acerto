[package]
name = "rest-server"
version = "0.1.0"
edition.workspace = true

[dependencies]
common = { path = "../common" }
app-core = { path = "../core" }

sqlx = { workspace = true, features = ["migrate"] }
env_logger = { workspace = true }
tokio = { workspace = true }

axum = { version = "0.7.4", default-features = false, features = ["tokio", "http2"] }
tower-http = { version = "0.5.1", default-features = false, features = ["cors"] }
http = { version = "1.0.0" }
log = "0.4.20"